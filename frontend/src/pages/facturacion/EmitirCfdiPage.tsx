import { PlaceholderPage } from "@/components/common/PlaceholderPage"

export function EmitirCfdiPage() {
  return (
    <PlaceholderPage
      title="Emitir Nuevo CFDI"
      description="Wizard de emisión: tipo → receptor → conceptos → método/forma → timbrado PAC"
    />
  )
}
